def combinatorial_circuit_generation():
    '''
    You could probably get
    Returns:

    '''
    # We need to take all of the inputs and outputs and generate all possible
    # variations of it. We might want to use a generator pattern to keep the
    # space down.


    # We then run the truth table through them.